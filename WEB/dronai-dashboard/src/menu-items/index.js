import { dashboard } from './dashboard';
import { utilities } from './utilities';
import { settings } from './settings';

//-----------------------|| MENU ITEMS ||-----------------------//

const menuItems = {
    items: [dashboard, utilities, settings]
};

export default menuItems;